window.addEventListener('resize', () => {
    document.querySelector(".hi").style.height = `${window.innerHeight}px`
    document.querySelector("section").style.height = `${window.innerHeight}px`
    document.querySelector(".hum").style.height = `${window.innerHeight}px`
  });